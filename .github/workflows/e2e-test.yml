name: E2E Test & Report

# DISABLED: Temporarily disabled during iterative development
# To re-enable: remove the 'if: false' condition from all jobs
on:
  workflow_dispatch:
    inputs:
      axon_version:
        description: 'Axon version to test'
        required: false
        default: 'v3.0.0'
      core_version:
        description: 'Core version to test'
        required: false
        default: '3.1.6-alpha'
  # Disabled triggers:
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]
  # repository_dispatch:
  #   types: [core-release-published]
  # schedule:
  #   - cron: '0 * * * *'

permissions:
  contents: write
  pages: write
  id-token: write
  packages: read

jobs:
  vet:
    name: Go Vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      
      - name: Run go vet
        run: go vet ./...
  
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v3
        with:
          version: v1.64.8
          args: --timeout=5m
  
  e2e-test:
    name: E2E Test
    environment:
      name: github-pages
    runs-on: ubuntu-latest
    needs: [vet, lint]
    steps:
      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h
          
          # Remove unnecessary software to free up space (~10GB)
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo apt-get clean
          
          echo "Disk space after cleanup:"
          df -h
      
      - uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl tar
      
      - name: Ensure Docker is accessible
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🐳 Docker Setup Check"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          docker --version || echo "⚠️  Docker CLI not found"
          docker ps 2>&1 || {
            echo "⚠️  Docker daemon not accessible"
            echo "   This may cause Axon to skip ONNX conversion"
          }
          echo "Current user: $(whoami)"
          echo "User groups: $(groups)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      
      - name: Install gh CLI
        run: |
          type -p curl >/dev/null || (apt-get update && apt-get install curl -y)
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg \
          && sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg \
          && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
          && sudo apt-get update \
          && sudo apt-get install gh -y
      
      - name: Authenticate gh CLI
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token
      
      - name: Determine Core Version
        id: core_version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # Manual trigger: use input or default
            VERSION="${{ github.event.inputs.core_version || '3.0.0-alpha' }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "Using workflow_dispatch version: $VERSION"
          elif [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            # Triggered by core-releases: use payload version
            VERSION="${{ github.event.client_payload.version }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "Using dispatch version: $VERSION"
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            # Scheduled: check for latest release in core-releases
            echo "Checking for latest release in core-releases..."
            LATEST_RELEASE=$(gh release list --repo mlOS-foundation/core-releases --limit 1 --json tagName --jq '.[0].tagName' || echo "")
            if [ -n "$LATEST_RELEASE" ] && [ "$LATEST_RELEASE" != "null" ]; then
              VERSION="$LATEST_RELEASE"
              echo "version=$VERSION" >> $GITHUB_OUTPUT
              echo "Found latest release: $VERSION"
            else
              echo "No releases found in core-releases, skipping"
              echo "version=" >> $GITHUB_OUTPUT
            fi
          else
            # Push/PR: use hardcoded version
            VERSION="3.0.0-alpha"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "Using default version for push/PR: $VERSION"
          fi
      
      - name: Skip if no version
        if: steps.core_version.outputs.version == ''
        run: |
          echo "No version determined, skipping E2E tests"
          exit 0
      
      - name: Build test tool
        run: |
          go build -o bin/e2e-test ./cmd/e2e-test
      
      - name: Check disk space before tests
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Disk Space Status:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          df -h
      
      - name: Run E2E tests
        if: steps.core_version.outputs.version != ''
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🧪 Running E2E Tests (Minimal Mode)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Axon: ${{ github.event.inputs.axon_version || 'v3.0.0' }}"
          echo "📦 Core: ${{ steps.core_version.outputs.version }} (from public core-releases)"
          echo ""
          ./bin/e2e-test \
            -axon-version ${{ github.event.inputs.axon_version || 'v3.0.0' }} \
            -core-version ${{ steps.core_version.outputs.version }} \
            -minimal
        continue-on-error: true
      
      - name: Upload HTML report as artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-report-${{ github.run_number }}
          path: e2e-results-*/release-validation-report.html
          retention-days: 90
      
      - name: Display Core logs if test failed
        if: failure()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Core Server Logs (if available)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          LOG_DIR=$(find . -type d -path "*/e2e-results-*/mlos-core/logs" | head -1)
          if [ -n "$LOG_DIR" ]; then
            if [ -f "$LOG_DIR/core-stdout.log" ]; then
              echo ""
              echo "📄 Core stdout:"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              tail -100 "$LOG_DIR/core-stdout.log" || echo "Could not read stdout log"
            fi
            if [ -f "$LOG_DIR/core-stderr.log" ]; then
              echo ""
              echo "📄 Core stderr:"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              tail -100 "$LOG_DIR/core-stderr.log" || echo "Could not read stderr log"
            fi
          else
            echo "⚠️  Core log directory not found"
          fi
      
      - name: List available logs
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Available Log Files"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          find e2e-results-* -type f \( -name "*.log" -o -name "core-*.log" \) 2>/dev/null | while read f; do
            echo "  📄 $f ($(du -h "$f" | cut -f1))"
          done || echo "  ⚠️  No log files found"
      
      - name: Upload logs as artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-logs-${{ github.run_number }}
          path: |
            e2e-results-*/mlos-core/logs/**
            e2e-results-*/test.log
          retention-days: 30
          if-no-files-found: ignore
      
      - name: Prepare report for GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && always()
        run: |
          mkdir -p _site
          REPORT_DIR=$(find . -type d -name "e2e-results-*" | head -1)
          if [ -n "$REPORT_DIR" ] && [ -f "$REPORT_DIR/release-validation-report.html" ]; then
            cp "$REPORT_DIR/release-validation-report.html" _site/index.html
            if [ -f "$REPORT_DIR/report_app.js" ]; then
              cp "$REPORT_DIR/report_app.js" _site/report_app.js
            fi
            echo "✅ Report prepared for GitHub Pages"
          else
            echo "⚠️ No report found - using fallback report"
            cp .github/fallback-report.html _site/index.html
            echo "✅ Fallback report copied"
          fi
      
      - name: Setup Pages
        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && always()
        uses: actions/configure-pages@v4
      
      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site
      
      - name: Deploy to GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && always()
        id: deployment
        uses: actions/deploy-pages@v4
      
      - name: Test Summary
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 E2E Test Complete"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          REPORT_DIR=$(find . -type d -name "e2e-results-*" | head -1)
          if [ -n "$REPORT_DIR" ] && [ -f "$REPORT_DIR/release-validation-report.html" ]; then
            echo "✅ Report generated"
            if [ "${{ github.ref }}" = "refs/heads/main" ] && [ "${{ github.event_name }}" = "push" ]; then
              echo "🌐 Report published to: https://mlos-foundation.github.io/system-test/"
            else
              echo "📦 Report available in artifacts (e2e-report-${{ github.run_number }})"
            fi
          else
            echo "⚠️  Report not generated (test may have failed)"
          fi

